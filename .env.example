# Preferred live LLM for agent decisions (free tier available at console.groq.com).
GROQ_API_KEY=
# Optional model override (default: llama-3.3-70b-versatile).
GROQ_MODEL=
# Fallback live LLM (only used if GROQ_API_KEY is unset).
OPENAI_API_KEY=
